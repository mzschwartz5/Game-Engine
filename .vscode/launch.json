{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Clang Compile and Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/GameEngineExecutable",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "MakeExecutable",
            "MIMode": "lldb",
        }
    ]
}