{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "MakeExecutable",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}